# 📋 สรุปการเปลี่ยนแปลงโปรเจค CONSTRUCTSHOP

## 🗄️ ฐานข้อมูล (Database)
| สิ่งที่ทำ | ไฟล์ |
| :--- | :--- |
| แก้ชื่อตาราง FOREIGN KEY ผิด (`tb_categories` → `categories`) | `Database.sql` |
| เพิ่ม `customer_type` column ให้ตาราง `customers` | phpMyAdmin SQL |
| สร้าง Mock Data ครบทุกตาราง (พนักงาน/สินค้า/บิล/ลูกค้า 20+ รายการ) | `mockdata.sql` |

---

## 👤 ระบบพนักงาน (Employees)
| สิ่งที่ทำ | ไฟล์ |
| :--- | :--- |
| แก้ bug role `'admin'` → `'Admin'` เพื่อให้สิทธิ์เข้าหน้า Manage Users | `manageuser.php` |
| เปลี่ยน Role field จาก input text → **Dropdown** (Admin/Manager/Cashier/Stock) | `addemployees.php` |
| แก้ไข Label ลอยของฟอร์มเพิ่มพนักงานให้แสดงผลถูกต้อง | `addemployees.php` |
| เพิ่มปุ่ม **"รีเซ็ตรหัสผ่าน"** (สีฟ้า) | `manageuser.php` |
| สร้างหน้า **รีเซ็ตรหัสผ่านพนักงาน** (UI ใหม่ทั้งหมด) | `reset_password.php` (ใหม่) |

---

## 🛍️ ระบบสินค้า (Products)
| สิ่งที่ทำ | ไฟล์ |
| :--- | :--- |
| เพิ่มฟีเจอร์ **อัปโหลดรูปภาพสินค้า** พร้อมระบบ Preview | `edit_material.php` |
| เปลี่ยนปุ่ม Input File เป็น **ปุ่มสีส้มดีไซน์สวยงาม** แสดงชื่อไฟล์และรูปตัวอย่างก่อนบันทึก | `edit_material.php` |

---

## 🏪 ระบบขาย/เบิกสินค้า (Stock Out)
| สิ่งที่ทำ | ไฟล์ |
| :--- | :--- |
| **แก้ Bug สำคัญ:** เพิ่มการตรวจสอบจำนวนสินค้าใน Stock ก่อนบันทึก | `stock_out.php` |
| หากเบิกเกินจำนวนที่มี: ระบบจะ **ยกเลิกบิลอัตโนมัติ** และแจ้งเตือนชื่อสินค้าที่ Stock ไม่พอ | `stock_out.php` |

---

## 👥 ระบบลูกค้า (Customers)
| สิ่งที่ทำ | ไฟล์ |
| :--- | :--- |
| แก้ไข Error: `Warning Undefined array key 'customer_type'` | `customers_list.php` |

---

---

# 📋 สรุปการเพิ่มฟีเจอร์ครั้งที่ 2

## 1. 🔄 อัปเดตสถานะบิล: ค้างชำระ → ชำระแล้ว
**ไฟล์ที่แก้ไข:** `salesofday.php`

**รายละเอียดสิ่งที่เพิ่ม:**
*   **PHP Handler:** เพิ่มระบบรับค่า `?confirm_pay=<sale_id>` เพื่อทำการ `UPDATE` สถานะในฐานข้อมูลทันทีเมื่อมีการกดยืนยัน
*   **ปุ่มยืนยันการชำระเงิน:** 
    *   บิลที่สถานะเป็น **"ค้างชำระ"** จะแสดงปุ่ม 🟢 **"ยืนยันชำระเงิน"** ใต้ Badge สถานะ
    *   บิลที่สถานะเป็น **"ชำระแล้ว"** ระบบจะซ่อนปุ่มนี้โดยอัตโนมัติ
*   **ระบบ UX:** เพิ่ม JavaScript Dialog เพื่อให้ยืนยันก่อนเปลี่ยนสถานะ พร้อมแสดง Alert แจ้งเตือนเมื่อดำเนินการสำเร็จ

---

## 2. 📊 สรุปการเงินบนหน้า Dashboard
**ไฟล์ที่แก้ไข:** `index.php`

**รายละเอียดสิ่งที่เพิ่ม (ส่วน Financial Summary):**

| การ์ดสรุปผล | แหล่งข้อมูล SQL / การคำนวณ |
| :--- | :--- |
| **💚 รายรับรวม** | `SUM(total_amount)` จากตาราง `sales` ที่มีสถานะเป็น `'ชำระแล้ว'` |
| **🔴 รายจ่ายรวม** | `SUM(qty × cost_price)` จากตาราง `receiving_detail` |
| **🟡 กำไร/ขาดทุน** | คำนวณจาก **[รายรับ - รายจ่าย]** (แสดงตัวเลขสีเขียว = กำไร / สีแดง = ขาดทุน) |

**ฟีเจอร์แจ้งเตือนเพิ่มเติม:**
*   **ระบบ Warning:** หากมียอดค้างชำระในระบบ จะแสดง **แถบแจ้งเตือนสีส้ม** แจ้งจำนวนบิลที่ค้างและยอดเงินรวมทั้งหมด
*   **Quick Link:** มีปุ่มลิงก์จากหน้า Dashboard ไปยังหน้ารายการขายเพื่อจัดการบิลที่ค้างได้ทันที

---

---

# 📋 สรุปการเพิ่มฟีเจอร์ครั้งที่ 3

## 📄 อัปเกรดใบเสร็จและระบบพิมพ์ PDF
ปรับปรุงดีไซน์ใบเสร็จให้สวยงามและรองรับการบันทึกเป็น PDF อย่างสมบูรณ์

**ไฟล์ที่แก้ไข:** `check_bill.php` และ `view_bill.php`

### ✨ สิ่งที่เปลี่ยนแปลงและพัฒนาเพิ่ม:

*   🎨 **Header Gradient:** ปรับแต่งส่วนหัวด้วยสีเข้ม-แดง พร้อมแสดงชื่อร้าน ข้อมูลติดต่อ และเลขที่บิลอย่างชัดเจน
*   📋 **Info Panel:** ส่วนแสดงข้อมูลลูกค้าและสถานะการชำระเงินในรูปแบบ **Badge สี (เขียว/แดง)** เพื่อให้แยกแยะสถานะได้ง่าย
*   📊 **ตารางรายการสินค้า:**
    *   หัวตารางใช้สี **Dark Navy**
    *   แถวตารางแบบสลับสี (Striped) เพื่อให้อ่านง่าย
    *   จัดตำแหน่งตัวเลขชิดขวาตามมาตรฐานบัญชี
*   ✍️ **Signature Block:** เพิ่มช่องสำหรับลงลายเซ็น **"ผู้รับสินค้า/ลูกค้า"** และ **"เจ้าหน้าที่"** ที่ส่วนท้ายของใบเสร็จ
*   🖨️ **ตั้งค่า @media print (เพื่อการพิมพ์ที่สมบูรณ์):**
    *   ซ่อนองค์ประกอบที่ไม่จำเป็น เช่น Navbar, Sidebar และ Footer ของเว็บไซต์อัตโนมัติขณะพิมพ์
    *   ตั้งค่าหน้ากระดาษเป็นขนาด **A4** พร้อมกำหนด Margin ที่เหมาะสม
    *   ใช้คำสั่ง `-webkit-print-color-adjust: exact` เพื่อให้สีและ Gradient แสดงผลถูกต้องเมื่อบันทึกเป็นไฟล์ PDF

---

### 💡 วิธีใช้งาน:
กดปุ่ม **"พิมพ์ / บันทึกเป็น PDF"** จากนั้นเลือกเครื่องพิมพ์เป็น **"Save as PDF"** ใน Browser Dialog เพื่อบันทึกไฟล์ใบเสร็จขนาด 1 หน้า A4 ได้ทันที

---

## 3.1 🏷️ แก้ไขสีข้อความ Header ใบเสร็จ
**ไฟล์ที่แก้ไข:** `check_bill.php`, `view_bill.php`

*   **รายละเอียด:** ปรับปรุงความคมชัดของข้อความในส่วน Header ที่เป็น Gradient
*   **การเปลี่ยนแปลง:** เปลี่ยนจากค่า `opacity: 0.8 / 0.85` เป็น `color: #fff !important`
*   **ผลลัพธ์:** ชื่อร้าน, ที่อยู่, วันที่ และชื่อผู้เปิดบิล แสดงผลเป็นสีขาวชัดเจน อ่านง่ายบนพื้นหลังสีเข้ม

---

---

# 📋 สรุปการเพิ่ม/แก้ไข ครั้งที่ 4

## 1. 🎁 ปรับปรุงเงื่อนไขโปรโมชั่น (แถมเสื้อทุก ฿10,000)
**ไฟล์ที่แก้ไข:** `check_bill.php`, `view_bill.php`

*   **รายละเอียด:** เปลี่ยนเงื่อนไขการแจกของแถมจาก "ขั้นต่ำ 10,000 ได้ 1 ตัว" เป็นการคำนวณตามสัดส่วนจริง
*   **ตรรกะใหม่:** ใช้สูตร `floor(ยอดรวม ÷ 10,000)` เพื่อคำนวณจำนวนของแถมตามยอดซื้อจริง ดังนี้:

| ยอดรวมสุทธิ | จำนวนเสื้อที่ได้รับ |
| :--- | :--- |
| ต่ำกว่า 10,000 บาท | ไม่ได้รับของแถม |
| 10,000 – 19,999 บาท | 🎽 1 ตัว |
| 20,000 – 29,999 บาท | 🎽 2 ตัว |
| 95,000 บาท | 🎽 9 ตัว |

---

## 2. 🖤 ปรับสีช่องกรอกราคาต่อหน่วย (Dark Theme)
**ไฟล์ที่แก้ไข:** `stock_out.php`

*   **รายละเอียด:** ปรับปรุง UI ของช่องกรอกข้อมูลราคาในหน้าระบบขาย/เบิกสินค้า
*   **การเปลี่ยนแปลง:** เปลี่ยนพื้นหลังช่องราคาต่อหน่วยจากสีขาว เป็นสี **#1a1a2e (ดำเข้ม)** และตัวเลขสีขาว เพื่อให้เข้ากับธีมหลักของเว็บไซต์

---

## 3. 🔃 ระบบเรียงลำดับ (Sort) ราคาขายและสินค้าคงเหลือ
**ไฟล์ที่แก้ไข:** `products_list.php`

*   **รายละเอียด:** เพิ่มความสะดวกในการบริหารจัดการสต็อกสินค้า
*   **ฟีเจอร์ใหม่:**
    *   หัวตาราง **"ราคาขาย"** และ **"คงเหลือ"** สามารถคลิกเพื่อเรียงลำดับได้
    *   แสดงสถานะด้วยลูกศร: **▲ (น้อยไปมาก)** / **▼ (มากไปน้อย)**
    *   ระบบสลับทิศทางอัตโนมัติเมื่อกดซ้ำที่หัวข้อเดิม
    *   **Security:** ป้องกัน SQL Injection ด้วยระบบ Whitelist สำหรับฟิลด์ที่ใช้เรียงลำดับ

---

---

# 📋 สรุปการแก้ไขครั้งที่ 5

## 1. 🔍 ระบบตรวจสอบบิล (`check_bill.php`)
*   **การเปลี่ยนแปลง:** ปรับปรุงหน้าตรวจสอบสถานะบิลให้ง่ายขึ้น
*   **รายละเอียด:** ค้นหาด้วย **เลขที่บิลอย่างเดียว** (นำช่องกรอกเบอร์โทรศัพท์ออก)

---

## 2. 💾 ระบบส่วนลด (Discount System)
**ไฟล์ SQL:** `discount_system.sql`
*   **โครงสร้างฐานข้อมูล:**
    *   สร้างตาราง `coupons` ใหม่เพื่อรองรับระบบคูปอง
    *   เพิ่มคอลัมน์ในตาราง `sales`: `subtotal_amount`, `discount_amount`, `discount_type`, `coupon_id`
    *   เพิ่มคูปองตัวอย่าง 4 รูปแบบ: `CASH100`, `CASH500`, `CASH1000`, `PCT10`

### 🆕 หน้าจัดการคูปอง (`manage_coupons.php`)
*   สิทธิ์ **Admin/Manager** สามารถ เพิ่ม/ลบ คูปองได้
*   **ประเภทคูปอง:**
    *   ส่วนลดเงินสด (฿100 - ฿1,000)
    *   ส่วนลดเปอร์เซ็นต์ (1% - 10%, กำหนดเพดานส่วนลดสูงสุด ฿10,000)
*   เพิ่มระบบ Popup แจ้งเตือนเมื่อกรอกข้อมูลเกินขอบเขตที่กำหนด
*   แก้ไขบั๊กการ Submit คูปองประเภทเปอร์เซ็นต์ (HTML5 min/max conflict)

### 🛒 หน้าขายสินค้า (`stock_out.php`)
*   เพิ่มระบบ **AJAX** ตรวจสอบรหัสคูปองแบบ Real-time
*   สิทธิ์ **Manager/Admin** สามารถกรอกส่วนลดแบบกำหนดเอง (Manual) ได้ (ไม่เกิน 10% หรือ ฿50,000)
*   แสดงการคำนวณยอดชัดเจน: **ยอดรวม (Subtotal) → ส่วนลด (Discount) → ยอดสุทธิ (Net Total)**

### 📄 หน้าใบเสร็จ (`view_bill.php`, `check_bill.php`)
*   แสดงรายละเอียด ยอดรวม / ส่วนลด (ระบุประเภท) / ยอดสุทธิ
*   **เงื่อนไขเสื้อแถม:** คำนวณจาก **"ยอดรวมก่อนหักส่วนลด"** เพื่อสิทธิประโยชน์ของลูกค้า

---

## 3. 🖼️ ปุ่มอัปโหลดรูปภาพ (`add_products.php`)
*   **UI ใหม่:** เปลี่ยนจากปุ่มมาตรฐานของ Browser (สีเทา) เป็น **ปุ่มสีส้มดีไซน์สวย**
*   **ฟีเจอร์:** เพิ่มระบบ Preview รูปภาพสินค้าก่อนกดบันทึก

---

## 4. 👤 รูปโปรไฟล์และข้อมูลส่วนตัว (`profile.php`)
*   พนักงานทุก Role สามารถอัปโหลดรูปโปรไฟล์ของตนเองได้
*   แสดงผลรูปภาพทรงวงกลม พร้อมขอบสีม่วง
*   **ระบบ Auto-Avatar:** หากยังไม่ได้อัปโหลดรูป ระบบจะสร้างรูปอวตารจากชื่อพนักงานให้อัตโนมัติ
*   ⚠️ **คำสั่ง SQL ที่ต้องรัน:** 
    ```sql
    ALTER TABLE employees ADD avatar VARCHAR(255) DEFAULT NULL;
    ```

---

## 5. 🔐 แก้ไขช่องโหว่การเข้าถึง (RBAC)
ปรับปรุงระบบจัดการสิทธิ์ (Role-Based Access Control) ให้รัดกุมขึ้น:

| ไฟล์ | สิทธิ์การเข้าถึง (หลังแก้ไข) |
| :--- | :--- |
| `addemployees.php` | **Admin เท่านั้น** (เดิมทุกคนเข้าได้) |
| `stock_in.php` | Admin + Stock |
| `add_products.php` | Admin + Manager + Stock |
| `edit_material.php` | Admin + Manager + Stock |
| `sidenav.php` | ซ่อน/แสดงเมนูตามสิทธิ์จริงของ User |

### ดังรูปภาพ excel table
<img width="810" height="514" alt="Image" src="https://github.com/user-attachments/assets/88059bda-fc67-4baa-aa5f-baab7f5e3e1e" />

* **Admin:** เข้าถึงทุกฟังก์ชันของระบบ
* **Manager:** จัดการสินค้า ดูรายงาน อนุมัติส่วนลด และแก้ไขงานขายบางส่วนได้
* **Cashier:** ทำงานขาย รับชำระเงิน ใช้คูปอง และดูประวัติบิลได้
* **Stock:** รับสินค้า เช็คสต็อก เบิกสินค้าออก และดูแลข้อมูลสินค้าคงคลังได้

---

## 6. 🛍️ แค็ตตาล็อกสินค้าสำหรับลูกค้า (`store.php`)
*   **ระบบเรียงลำดับ (Sort):** ใหม่ล่าสุด / ราคา (สูง-ต่ำ, ต่ำ-สูง) / ชื่อ (ก-ฮ, ฮ-ก)
*   **ระบบค้นหา:** เพิ่มช่อง Search ค้นหาชื่อสินค้าในหน้าร้านค้า
*   **ข้อมูลเพิ่มเติม:** แสดงจำนวนสินค้าที่ค้นพบ

---

## 7. 📊 จัดการรายการสินค้าหลังบ้าน (`products_list.php`)
*   เพิ่มความสามารถในการ Sort ข้อมูลได้ **ทุกคอลัมน์**:
    *   รหัสสินค้า / ชื่อ / หมวดหมู่ / ราคาขาย / จำนวนคงเหลือ (▲▼)

---

---

# 📋 สรุปการแก้ไขครั้งที่ 6

## 1. 📦 ระบบการแปลงหน่วย (Unit Conversion / Packaging System)
ออกแบบและพัฒนาระบบ Packaging ตามมาตรฐาน ERP รองรับการรับสินค้าแบบหลายหน่วยต่อสินค้าเดียว

> ⚠️ **ต้องรัน `Database/packaging_system.sql` ใน phpMyAdmin ก่อนใช้งาน**

### 🗄️ ฐานข้อมูล (`packaging_system.sql`)
- เพิ่มคอลัมน์ `base_unit` ในตาราง `products` เพื่อระบุหน่วยหลักของ stock (เช่น ถุง, เส้น, ตัว)
- สร้างตาราง `product_packaging` พร้อม `UNIQUE(product_id, package_unit)` และ `INDEX(product_id)`
- รองรับหลาย Packaging ต่อสินค้า 1 ชนิด

| สินค้า | Base Unit | Packaging | Rate | ผลลัพธ์ |
| :--- | :--- | :--- | :--- | :--- |
| ปูน | ถุง | พาเหรด | 40 | 3 พาเหรด = 120 ถุง |
| ตะปู | ตัว | ลัง | 2000 | 2 ลัง = 4000 ตัว |

### 🆕 AJAX Endpoint (`get_packaging.php`)
- รับ `?product_id=X` → return JSON พร้อม label **"1 พาเหรด = 40 ถุง"**

### ✏️ เพิ่มสินค้า (`add_products.php`)
- เพิ่มช่อง **"หน่วยหลัก (Base Unit)"** แยกจากช่องหน่วยนับ
- เพิ่ม Section Packaging (optional): เพิ่มสูตรได้หลายแถว พร้อม preview real-time สีเขียว
- บันทึกลง `product_packaging` อัตโนมัติหลัง INSERT สินค้า

### 📥 รับสินค้าเข้า (`stock_in.php`)
- เลือกสินค้า → AJAX ดึง Packaging options อัตโนมัติ
- **Toggle "ใช้หน่วย Packaging"** ปรากฏเมื่อสินค้ามี Packaging กำหนดไว้
- แสดงผลคำนวณ real-time: `3 พาเหรด × 40 = 120 ถุง`
- PHP แปลง `pkg_qty × rate → base_unit` ก่อนบันทึก stock

---

## 2. 🗑️ ปุ่มถังขยะ (Delete Buttons)

| หน้า | ผู้มีสิทธิ์ | พิเศษ |
| :--- | :--- | :--- |
| `products_list.php` | ตามสิทธิ์ | Cascade ลบ `receiving_detail` + `sales_detail` ก่อน |
| `receiving_history.php` | Admin | ลบ `receiving_detail` + header พร้อมกัน |
| `salesofday.php` | Admin / Manager | **คืน Stock อัตโนมัติ** ทุกรายการในบิลก่อนลบ |

ทุกปุ่มแสดง **confirm popup** ก่อนดำเนินการ โดยใช้ `data-*` attributes เพื่อป้องกัน JS quote error

---

## 3. 🐛 Bug Fix

| ปัญหา | สาเหตุ | วิธีแก้ |
| :--- | :--- | :--- |
| ลบสินค้า → Fatal Error (FK constraint) | PHP ใหม่ throw exception แทน return false | ครอบ `try/catch` ทุก DELETE |
| Confirm popup ไม่แสดง — ลบทันที | Single quote ในชื่อสินค้าทำ JS broke | เปลี่ยนเป็น `data-*` attributes + `addEventListener` |
| Packaging rate ไม่ถูกส่งไป PHP | Inject `<input>` ใน `<tr>` ตอน submit — browser ไม่รวม | ใส่ hidden input ใน row ตั้งแต่ต้น, JS update `.value` เมื่อเลือก |

---
